{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm config set cache /dev/null && npm ci && npm run build && mkdir -p backend/public && cp -r dist/* backend/public/"
  },
  "deploy": {
    "startCommand": "cd backend && npm config set cache /dev/null && npm ci && node server.js"
  }
}
