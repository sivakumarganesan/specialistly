{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm ci --force && npm run build && mkdir -p backend/public && cp -r dist/* backend/public/ && cd backend && npm ci --force"
  },
  "deploy": {
    "startCommand": "cd backend && node server.js"
  }
}
