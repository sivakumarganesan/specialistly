{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm ci --prefer-offline --no-audit && npm run build && mkdir -p backend/public && cp -r dist/* backend/public/ && cd backend && npm ci --prefer-offline --no-audit"
  },
  "deploy": {
    "startCommand": "cd backend && node server.js"
  }
}
